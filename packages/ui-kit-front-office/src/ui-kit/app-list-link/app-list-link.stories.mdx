import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import AppListLink from './app-list-link.vue';

<Meta
  title="ui-kit/AppListLink"
  component={AppListLink}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppListLink },
  template: `
      <app-list-link
        tag="a"
        to="/"
        icon="icon-account"
        title="Title"/>
    `,
});

## AppListLink

Example:

<Source
  code={`
<app-list-link
    to="/"
    icon="icon-account"
    title="Title"/>
`}
/>

### Default

<Canvas>
  <Story name="Default view">{Template.bind({})}</Story>
</Canvas>

## Props

<ArgsTable story="Default view" />

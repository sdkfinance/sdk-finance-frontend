import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import AppShowMoreButton from './app-show-more-button.vue';
const argTypes = {
  totalCount: { control: { type: 'number', min: 0 } },
  currentCount: { control: { type: 'number', min: 0 } },
};

<Meta
  title="ui-kit/AppShowMoreButton"
  component={AppShowMoreButton}
  argTypes={argTypes}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppShowMoreButton },
  template: `
      <app-show-more-button v-bind="{totalCount, currentCount}"></app-show-more-button>`,
});

### Default

<Canvas>
  <Story
    name="Default view"
    args={{ totalCount: 10000, currentCount: 0 }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={AppShowMoreButton} />

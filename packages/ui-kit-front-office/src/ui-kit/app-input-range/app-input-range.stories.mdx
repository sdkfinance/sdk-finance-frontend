import AppInputRange from './app-input-range.vue';
import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';

<Meta
  title="ui-kit/AppInputRange"
  component={AppInputRange}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppInputRange },
  data() {
    return {
      inputRangeModel: {
        min: undefined,
        max: undefined,
      },
    };
  },
  template: `<app-input-range v-model="inputRangeModel"/>`,
});

## AppInputRange

Example:

<Source code={`<app-input-range v-model="inputRangeModel"/>`} />

### Default

<Canvas>
  <Story name="Default view">{Template.bind({})}</Story>
</Canvas>

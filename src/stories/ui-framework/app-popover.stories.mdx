import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs';
import AppPopover from '@/components/ui-framework/app-popover.vue';


<Meta title="ui-framework/AppPopover" component={AppPopover} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppPopover },
  template: `
      <app-popover
        :placement="placement"
        :trigger="trigger"
        :width="width">
        Some content
        <template #action>
          <span>Trigger element</span>
        </template>
      </app-popover>
    `,
});


## AppPopover

Example:
<Source code={`
  <app-popover
    :placement="placement"
    :trigger="trigger"
    :width="width">
    Some content
    <template v-slot:action>
      <span>Trigger element</span>
    </template>
  </app-popover>
`}/>


### Default
<Canvas>
  <Story name="Default view" args={{ width: 300, placement: 'top', trigger: 'hover' }}>
    { Template.bind({}) }
  </Story>
</Canvas>

### Bottom + trigger click
<Canvas>
  <Story name="Bottom + trigger click" args={{ width: 300, placement: 'bottom', trigger: 'click' }}>
    { Template.bind({}) }
  </Story>
</Canvas>

## List of placement
```js
[
    'top',
    'top-start',
    'top-end',
    'bottom',
    'bottom-start',
    'bottom-end',
    'left',
    'left-start',
    'left-end',
    'right',
    'right-start',
    'right-end',
]
```

## List of trigger
```js
[
    'click',
    'focus',
    'hover',
    'manual'
]
```


## Props
<ArgsTable story="Default view" />

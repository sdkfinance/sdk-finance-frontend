import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs';
import AppChartSummaryData from '@/components/ui-kit/app-charts/app-chart-summary-data.vue';
const additionalInfo = [
    {
        currency: 'BTC',
        percent: 50,
        amount: 21373.40,
        symbol: '$',
        color: '#b0670f',
    },
    {
        currency: 'USD',
        percent: 25,
        amount: 1373.40,
        symbol: '$',
        color: '#3fab54',
    },
    {
        currency: 'GBP',
        percent: 5,
        amount: 373,
        symbol: '$',
        color: '#1a6f9f',
    },
    {
        currency: 'USD',
        percent: 25,
        amount: 21373.40,
        symbol: '$',
        color: '#39c761',
    },
];

<Meta title="ui-kit/charts/AppChartSummaryData" component={AppChartSummaryData} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppChartSummaryData },
  template: `
      <div>
        <app-chart-summary-data :data="data" v-for="(data, i) in additionalInfo" :key="i" :is-row="isRow"/>
      </div>
    `,
});



## AppChartSummaryData

Example:
<Source code={
  ` <app-chart-summary-data :data="data" :is-row="isRow"/>
`}/>


### Default
<Canvas>
  <Story name="Default view" args={{ additionalInfo }}>
    { Template.bind({}) }
  </Story>
</Canvas>

### Row style
<Canvas>
  <Story name="Row style" args={{ additionalInfo, isRow: true }}>
    { Template.bind({}) }
  </Story>
</Canvas>


## Props
<ArgsTable story="Default view" />

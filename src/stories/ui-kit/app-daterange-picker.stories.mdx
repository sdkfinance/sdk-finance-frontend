import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import AppDaterangePicker from '@/components/ui-kit/app-daterange-picker.vue'

<Meta title="ui-kit/AppDaterangePicker" component={AppDaterangePicker}/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppDaterangePicker },
  template: `<app-daterange-picker :value="''"
    :date-range="[]"
    auto-apply
    :single-date-picker="false"
    :ranges="false"
    always-show-calendars/>`,
});


## AppDaterangePicker

Example:
<Source code={`<app-daterange-picker
    :value="value"
    :date-range="rangeValue"
    auto-apply
    :single-date-picker="visibilityModel"
    :ranges="false"
    always-show-calendars
    @update="emitChange"/>`}/>

Simple data-range picker.

### Default
<Canvas>
  <Story name="Default view" height="400px">
    { Template.bind({}) }
  </Story>
</Canvas>


## Props
<ArgsTable story="Default view" />

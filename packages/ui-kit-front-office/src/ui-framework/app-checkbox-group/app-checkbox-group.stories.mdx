import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs';
import AppCheckboxGroup from './app-checkbox-group.vue';
import AppCheckbox from '../app-checkbox/app-checkbox.vue';
const argTypes = {
  disabled: { control: { type: 'boolean' } },
};

<Meta
  title="ui-framework/AppCheckboxGroup"
  component={AppCheckboxGroup}
  argTypes={argTypes}
/>

export const Template = (args, { argTypes }) => ({
  components: { AppCheckboxGroup, AppCheckbox },
  props: Object.keys(argTypes),
  data() {
    return {
      modelValue: [],
    };
  },
  template: `
        <app-checkbox-group v-model="modelValue" :disabled="disabled">
          <app-checkbox label="label1">Label 1</app-checkbox>
          <app-checkbox label="label2">Label 2</app-checkbox>
          <app-checkbox label="label3">Label 3</app-checkbox>
        </app-checkbox-group>`,
});

### Default

<Canvas>
  <Story name="Default view">{Template.bind({})}</Story>
</Canvas>

### Disabled

<Canvas>
  <Story
    name="Disabled"
    args={{ disabled: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={AppCheckboxGroup} />

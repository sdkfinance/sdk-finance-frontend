import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import AppInput from '@/components/ui-framework/app-input.vue'

<Meta title="ui-framework/AppInput" component={AppInput} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppInput },
  data: ()=>({
    text: ''
  }),
  template: `
    <app-input
      v-model="text"
      :type="type"
      :label="label"
      :rows="rows"
      :minlength="minlength"
      :maxlength="maxlength"
      :placeholder="placeholder"
      :clearable="clearable"
      :small-text="smallText"
      :disabled="disabled">
    </app-input>
  `,
});


## AppInput

Example:
<Source code={`
  <app-input
    v-model="text"
    :type="type"
    :label="label"
    :rows="rows"
    :minlength="minlength"
    :maxlength="maxlength"
    :placeholder="placeholder"
    :clearable="clearable"
    :small-text="smallText"
    :disabled="disabled">
</app-input>`
}>
</Source>

### Default
<Canvas>
  <Story name="Default view" args={{
    type: 'text',
    minlength: '10',
    maxlength: '10',
    placeholder: 'Type text',
    clearable: false,
    disabled: false,
    smallText: false,
    label: '',
    rows: 2,
  }}>
    { Template.bind({}) }
  </Story>
</Canvas>

## Props
<ArgsTable story="Default view" />

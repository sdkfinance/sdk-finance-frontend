import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import AppRadioButton from './app-radio-button.vue';
import AppRadio from '../app-radio/app-radio.vue';
const argTypes = {
  size: { control: { type: 'radio', options: ['medium', 'large'] } },
  label: { control: { type: 'text' } },
  name: { control: { type: 'text' } },
  disabled: { control: { type: 'boolean' } },
};

<Meta
  title="ui-framework/AppRadioButton"
  component={AppRadioButton}
  argTypes={argTypes}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppRadioButton },
  template: `
      <app-radio-button :disabled="disabled" :name="name" :label="label" :size="size"></app-radio-button>`,
});

## AppRadioButton

Example:

<Source code={`<app-radio-button :disabled="disabled" :name="name" size="medium" :label="label"/>`}></Source>

### Default view

<Canvas>
  <Story
    name="Default view"
    args={{ label: 'Default view label', size: 'medium', disabled: false }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Default view (large)

<Canvas>
  <Story
    name="Default view (large)"
    args={{ label: 'Default view (large) label', size: 'large', disabled: false }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Disabled state

<Canvas>
  <Story
    name="Disabled state"
    args={{ label: 'Disabled state label', size: 'medium', disabled: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Checked state

<Canvas>
  <Story
    name="Checked state"
    args={{ label: 'Checked state label', size: 'medium', disabled: false }}>
    {{
      components: { AppRadioButton, AppRadio },
      props: Object.keys(argTypes),
      template: `
              <app-radio :value="label">
                <app-radio-button :disabled="disabled" :label="label" :size="size"/>
              </app-radio>`,
    }}
  </Story>
</Canvas>

### Checked disabled state

<Canvas>
  <Story
    name="Checked disabled state"
    args={{ label: 'Checked state label', size: 'medium', disabled: true }}>
    {{
      components: { AppRadioButton, AppRadio },
      props: Object.keys(argTypes),
      template: `
              <app-radio :value="label">
                <app-radio-button :disabled="disabled" :label="label" :size="size"/>
              </app-radio>`,
    }}
  </Story>
</Canvas>

## ArgsTable

<ArgsTable of={AppRadioButton} />

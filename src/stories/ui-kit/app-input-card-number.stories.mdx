import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import AppInputCardNumber from '@/components/ui-kit/app-input-card-number.vue'

<Meta title="ui-kit/AppInputCardNumber" component={AppInputCardNumber}/>

export const Template = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { AppInputCardNumber },
    template: `<app-input-card-number v-model="value"/>`,
});


## AppInputCardNumber

Example:
<Source code={`<app-input-card-number v-model="value"/>`}/>

### Default
<Canvas>
    <Story name="Default view" args={{ value: '', }}>
        { Template.bind({}) }
    </Story>
</Canvas>

## Props
<ArgsTable story="Default view" />

import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs';
import Modal from '@/components/modal.vue';
import AppButton from '@/components/ui-framework/app-button.vue';

<Meta title="others/Modal" component={Modal}/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Modal, AppButton },
  template: `
    <div>
      <modal v-model="isModalShow" :title="title" :width="width" :fixed-header="isFixedHeader">
        <p v-for="i in 100" :key="i">Your content</p>
      </modal>
      <app-button @click="isModalShow=true">Open modal</app-button>
    </div>
  `,
});


## Modal

Example:
<Source code={
  `<modal
    v-model="isModalShow"
    :title="title"
    :width="width"
    :fixed-header="isFixedHeader">
    Content
</modal>`
}></Source>

### Default
<Canvas style={{ position: 'relative' }}>
  <Story name="Default view" args={{ isModalShow: false, title: 'Title', width: '380px', isFixedHeader: false }}>
    { Template.bind({}) }
  </Story>
</Canvas>

### With fixed header
<Canvas>
  <Story name="Fixed header view" args={{ isModalShow: false, title: 'Title', width: '380px', isFixedHeader: true }}>
    { Template.bind({}) }
  </Story>
</Canvas>

## Props
<ArgsTable story="Default view" />

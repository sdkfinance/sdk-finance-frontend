import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs';
import AppChartLine from './app-chart-line.vue';
import AppChartSummaryData from './app-chart-summary-data.vue';
const chartData = {
  labels: ['1', '2', '3', '4', '5', '6'],
  datasets: [
    {
      data: [50, 25, 5, 25],
      backgroundColor: [
        'rgba(255, 99, 132, 1)',
        'rgba(54, 162, 235, 1)',
        'rgba(255, 206, 86, 1)',
        'rgba(75, 192, 192, 1)',
        'rgba(153, 102, 255, 1)',
        'rgba(255, 159, 64, 1)',
      ],
    },
  ],
};
const additionalInfo = [
  {
    currency: 'BTC',
    percent: 50,
    amount: 21373.4,
    symbol: '$',
    color: '#b0670f',
  },
  {
    currency: 'USD',
    percent: 25,
    amount: 1373.4,
    symbol: '$',
    color: '#3fab54',
  },
  {
    currency: 'GBP',
    percent: 5,
    amount: 373,
    symbol: '$',
    color: '#1a6f9f',
  },
  {
    currency: 'USD',
    percent: 25,
    amount: 21373.4,
    symbol: '$',
    color: '#39c761',
  },
];

<Meta
  title="ui-kit/charts/AppChartLine"
  component={AppChartLine}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppChartLine },
  template: `
     <app-chart-line :chart-data="chartData" :additional-info="additionalInfo" >
        <template #label="scope">
          Some label {{ scope.currency }}
        </template>
      </app-chart-line>
    `,
});

export const TemplateWithSlot = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppChartLine, AppChartSummaryData },
  template: `
      <app-chart-line :chart-data="chartData" :additional-info="additionalInfo" >
        <template #label="scope">
          <app-chart-summary-data :data="scope"/>
        </template>
      </app-chart-line>
    `,
});

## AppChartLine

### This is the custom chart but api like for chart.js charts.

#### All documentation is [here](https://www.chartjs.org/)

Example:

<Source
  code={`<app-chart-line :chart-data="chartData" :additional-info="additionalInfo" >
    <template v-slot:label="scope">
      Some label
    </template>
  </app-chart-line>
`}
/>

### Default

<Canvas>
  <Story
    name="Default view"
    args={{ chartData, additionalInfo }}>
    {Template.bind({})}
  </Story>
</Canvas>

### With summary label

<Canvas>
  <Story
    name="With summary label"
    args={{ chartData, additionalInfo }}>
    {TemplateWithSlot.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Default view" />

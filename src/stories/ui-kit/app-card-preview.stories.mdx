import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs';
import AppCardPreview from '@/components/ui-kit/app-card-preview.vue';

<Meta title="ui-kit/AppCardPreview" component={AppCardPreview} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppCardPreview },
  template: `
      <app-card-preview
          :card-number="cardNumber"
          :exp-date="expirationDate"
          :full-name="name"/>
    `,
});


## AppCardPreview

Example:
<Source code={`
    <app-card-preview
      :card-number="cardNumber"
      :exp-date="expirationDate"
      :full-name="name"/>
`}/>



### Default
<Canvas>
  <Story name="Default view" args={{ cardNumber: '9348-0316-3277-7833', expirationDate: '8/24', name: 'Test name'}}>
    { Template.bind({}) }
  </Story>
</Canvas>


## Props
<ArgsTable story="Default view" />

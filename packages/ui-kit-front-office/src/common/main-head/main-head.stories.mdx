import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs';
import MainHead from './main-head.vue';
import AppButton from '../../ui-framework/app-button/app-button.vue';

<Meta
  title="others/MainHead"
  component={MainHead}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { MainHead, AppButton },
  template: `
    <main-head>
      <template #title>Title</template>
      <template #actions>
        <app-button size="small">Action button</app-button>
      </template>
    </main-head>
  `,
});

## MainHead

Example:

<Source
  code={`
 <main-head>
      <template #title>Title</template>
      <template #actions>Action</template>
</main-head>
`}></Source>

### Default

<Canvas>
  <Story
    name="Default view"
    style={{ width: '100%' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Default view" />

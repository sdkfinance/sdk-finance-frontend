import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import AppStepController from './app-step-controller.vue';

<Meta
  title="ui-kit/AppStepController"
  component={AppStepController}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppStepController },
  template: `
   <app-step-controller
        total-steps="2"
        back-name="/"
        :is-close-visible="isCloseVisible"
        :is-back-visible="isBackVisible">
        <template #default="{ currentStep, nextStep, goToBackPage }">
          <div class="max-w-540 ml-auto mr-auto">
            Current step is: {{ currentStep }} <button @click="nextStep"> Next step </button>
          </div>
        </template>
    </app-step-controller>`,
});

## AppStepController

Example:

<Source
  code={`
<app-step-controller
    total-steps="2"
    back-name="/"
    :is-close-visible="isCloseVisible"
    :is-back-visible="isBackVisible">
    <template #default="{ currentStep, nextStep, goToBackPage }">
      Current step is: {{ currentStep }} <button @click="nextStep"> Next step </button>
    </template>
</app-step-controller>
`}
/>

### Default

<Canvas>
  <Story
    name="Default view"
    args={{ value: '', isCloseVisible: true, isBackVisible: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Default view" />

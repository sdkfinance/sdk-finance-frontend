import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import AppStatsBadge from '@/components/ui-kit/app-stats-badge.vue'

<Meta title="ui-kit/AppStatsBadge" component={AppStatsBadge}/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppStatsBadge },
  template: `<app-stats-badge icon="icon-user">
                   <template #title>
                     Active customers <strong>500</strong>
                   </template>
                   <template #subtitle>
                     Сustomers <strong>150</strong>
                   </template>
                 </app-stats-badge>`,
});


## AppStatsBadge

Example:
<Source code={`<app-stats-badge icon="icon-user">
    <template #title>
        Active customers <strong>500</strong>
    </template>
    <template #subtitle>
        Сustomers <strong>150</strong>
    </template>
</app-stats-badge>`}></Source>

Simple badge, with one icon prop (accept full icon name) and two slots: title and subtitle.

### Default
<Canvas>
  <Story name="Default view">
    { Template.bind({}) }
  </Story>
</Canvas>

## Props
<ArgsTable story="Default view" />

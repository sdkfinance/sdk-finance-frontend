import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import AppInputHint from '@/components/ui-kit/app-input-hint.vue'

<Meta title="ui-kit/AppInputHint" component={AppInputHint}/>

export const Template = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { AppInputHint },
    template: `
      <app-input-hint />
    `,
});


## AppInputHint
#### For now, we use this hint only for password input
#### If you need use another hints, yow should improve this component and add some props or slots for it

Example:
<Source code={`
 <app-input-hint />
`}/>

### Default
<Canvas>
    <Story name="Default view">
        { Template.bind({}) }
    </Story>
</Canvas>

## Props
<ArgsTable story="Default view" />

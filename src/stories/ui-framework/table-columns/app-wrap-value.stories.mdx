import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import AppWrapValue from '@/components/ui-kit/table-columns/app-wrap-value.vue'

<Meta title="ui-kit/table-columns/AppWrapValue" component={AppWrapValue} />

export const Template = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { AppWrapValue },
    template: `<div style="width: 60px;"><app-wrap-value :value="value"/></div>`
});


## AppWrapValue
Wrapper for long text in table with copy functionality.

Example:
<Source code={`<app-wrap-value :value="value"/>`} />

### Default
<Canvas>
    <Story name="Default view" args={{ value: 'Some long text' }}>
        { Template.bind({}) }
    </Story>
</Canvas>


## Props
<ArgsTable story="Default view" />

import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import AppCardProductPreview from './app-card-product-preview.vue';
const argTypes = {
  cardPan: { control: { type: 'text' } },
  expiredDate: { control: { type: 'text' } },
  ownerName: { control: { type: 'text' } },
  backgroundImage: { control: { type: 'text' } },
};

<Meta
  title="ui-framework/AppCardProductPreview"
  component={AppCardProductPreview}
  argTypes={argTypes}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppCardProductPreview },
  template: `<app-card-product-preview v-bind="{cardPan,expiredDate,ownerName,backgroundImage}"/>`,
});

## AppCardProductPreview

Example:

<Source code={`<app-card-product-preview/>`}></Source>

### Default

<Canvas>
  <Story name="Default view">{Template.bind({})}</Story>
</Canvas>

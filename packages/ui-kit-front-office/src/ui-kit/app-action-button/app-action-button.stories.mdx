import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import AppActionButton from './app-action-button.vue';

<Meta
  title="ui-kit/AppActionButton"
  component={AppActionButton}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppActionButton },
  template: `<app-action-button
                :icon='icon'
                :title='title'
                :disabled='disabled'
              />`,
});

## AppActionButton

Example:

<Source code={`<app-action-button :icon="icon" :title="title" :disabled="disabled" />`}></Source>

### Default

<Canvas>
  <Story
    name="Default view"
    args={{ title: 'Custom title', icon: 'icon-transfer-from', disabled: false }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Disabled view

<Canvas>
  <Story
    name="Custom label view"
    args={{ title: 'Custom title', icon: 'icon-transfer-from', disabled: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Default view" />

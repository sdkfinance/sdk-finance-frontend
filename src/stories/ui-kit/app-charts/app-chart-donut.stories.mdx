import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs';
import AppChartDonut from '@/components/ui-kit/app-charts/app-chart-donut.vue';
const chartData = {
    labels: ['1', '2', '3', '4', '5', '6'],
    datasets: [{
        data: [12, 19, 0, 5, 2, 3],
        backgroundColor: [
            'rgba(255, 99, 132, 1)',
            'rgba(54, 162, 235, 1)',
            'rgba(255, 206, 86, 1)',
            'rgba(75, 192, 192, 1)',
            'rgba(153, 102, 255, 1)',
            'rgba(255, 159, 64, 1)'
        ],
    }]
};

<Meta title="ui-kit/charts/AppChartDonut" component={AppChartDonut} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppChartDonut },
  template: `
      <app-chart-donut :chart-data="chartData" :options="options" :height="height" :width="width"/>
    `,
});

export const TemplateWithSlot = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { AppChartDonut },
    template: `
      <app-chart-donut :chart-data="chartData" :options="options" :height="height" :width="width">
        This is a slot. You can past any data here.
      </app-chart-donut>
    `,
});


## AppChartDonut

### This is the wrapper for chart.js doughnut chart.
#### All documentation is [here](https://www.chartjs.org/)

Example:
<Source code={
  `<app-chart-donut :chart-data="chartData" :options="options" :height="height" :width="width"/>
`}/>


## Chart example
[Example](https://www.chartjs.org/docs/latest/charts/doughnut.html)

### Default
<Canvas>
  <Story name="Default view" args={{ chartData }}>
    { Template.bind({}) }
  </Story>
</Canvas>

### With default slot
<Canvas>
    <Story name="With default slot" args={{ chartData }}>
        { TemplateWithSlot.bind({}) }
    </Story>
</Canvas>



## Props
<ArgsTable story="Default view" />

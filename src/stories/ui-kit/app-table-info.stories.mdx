import { Meta, Story, Canvas, Source, ArgsTable } from '@storybook/addon-docs';
import AppTableInfo from '@/components/ui-kit/app-table/app-table-info.vue'

<Meta title="ui-kit/AppTableInfo" component={AppTableInfo}/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppTableInfo },
  template: `<app-table-info
                :image="image"
                :title="title"
                :time="time"
                :has-error="hasError"
                :has-shadow="hasShadow"
                :description="description"
                :is-summary="isSummary"
                :status="status"/>
            `,
});

## isSummary prop is set for Summary page only and can be viewed on mobile

## AppTableInfo

Example:
<Source code={
  `<app-table-info
    :image="image"
    :title="title"
    :time="time"
    :has-error="hasError"
    :has-shadow="hasShadow"
    :description="description"
    :is-summary="isSummary"
    :status="status"/>`
}/>

### Default with props
<Canvas>
  <Story name="Default state" args={{
    image: 'https://picsum.photos/200/300',
    title: 'McDonalds',
    time: new Date(),
    hasError: false,
    hasShadow: false,
    isSummary: false,
    description: 'Cafes & restaurants',
    status: 'pending'
  }}>
    { Template.bind({}) }
  </Story>
</Canvas>

### Error state
<Canvas>
  <Story name="Error state" args={{
    image: 'https://picsum.photos/200/300',
    title: 'McDonalds',
    time: new Date(),
    hasError: true,
    hasShadow: false,
    description: 'Cafes & restaurants',
    status: 'Rejected'
  }}>
    { Template.bind({}) }
  </Story>
</Canvas>

## Props
<ArgsTable story="Default view" />

import { ArgsTable, Canvas, Meta, Source, Story } from '@storybook/addon-docs';
import AppCustomSelectOption from './app-custom-select-option.vue';

<Meta
  title="ui-kit/selects/AppCustomSelectOption"
  component={AppCustomSelectOption}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { AppCustomSelectOption },
  template: `
      <app-custom-select-option
            color="red"
            :title="title"
            subtitle="999999999"
            :custom-initials="customInitials"
            :with-logo="withLogo"
            :is-action-available="isActionAvailable"/>
    `,
});

## AppCustomSelectOption

Example:

<Source
  code={`
 <app-custom-select-option
    color="red"
    :title="title"
    subtitle="999999999"
    :custom-initials="customInitials"
    :with-logo="withLogo"
    :is-action-available="isActionAvailable"/>
`}
/>

### Default

<Canvas>
  <Story
    name="Default view"
    args={{ isActionAvailable: true, withLogo: false, customInitials: '', title: 'Some title' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### With custom initials, example: UA

<Canvas>
  <Story
    name="With custom initials"
    args={{ isActionAvailable: true, withLogo: false, customInitials: 'UA', title: 'Some title' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Without delete action

<Canvas>
  <Story
    name="Without delete action"
    args={{ isActionAvailable: false, withLogo: false, customInitials: '', title: 'Some title' }}>
    {Template.bind({})}
  </Story>
</Canvas>

### With sdk logo

<Canvas>
  <Story
    name="With sdk logo"
    args={{ isActionAvailable: true, withLogo: true, customInitials: '', title: 'Some title' }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Default view" />
